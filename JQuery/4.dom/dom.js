$(document).ready(function(){

    // $('#tercera').parent().css({
    //     background: '#1b3b82'
    // }); //Cambiar estilos de CSS del elemento padra

    // $('#tercera').parents(); // Accedo a todos los elementos padres

    // $('#padre').children().fadeOut(3000); // Accedo a todos los elementos hijos
    // $('#padre').children('#tercera').fadeOut(3000); // Accedo a un hijo de id especifico

    //$('#contenedor').find('div.caja').slideUp(2000); 
    //Busca a partir de un contenedor un elemento espefico de los hijos

    //$('#tercera').siblings().fadeOut(); // Accede a todos los elementos hermanos de tercera 
    //excepto tercera

    // $('#tercera').prev().css({
    //     background: '#000'
    // }); // prev y next seleccionan al elemento anterior y posterior del que nos basemos

    // $('#tercera').prevAll().css({
    //     background: '#000'
    // }); // selecciona a todos los elementos anteriores
});