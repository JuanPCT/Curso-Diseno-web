$(document).ready(function(){
    //$(selector).action();
// con $ indicamos que vamos a acceder a jquery.
// selector - Accedemos al elemento que queramos trabajar 
// action - Lo que queremos hacer con el elemento seleccionado

    $('h1').hide();
});

// -------------
//Otra forma de hacerlo
$(function(){

});